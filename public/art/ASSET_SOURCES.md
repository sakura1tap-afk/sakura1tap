# Runtime Art Asset Sources

This folder contains optimized or directly shipped runtime copies of selected assets from the local `素材库/` staging folder.

## Models

| Runtime path | Source path | Use |
| --- | --- | --- |
| `/art/models/dna_hologram.glb` | `素材库/dna_hologram.glb` | Lazy main scene genome hologram |

## Notes

- The original staging files remain in `素材库/`.
- Large `.blend.zip` and `.exr` assets are not shipped here until converted or downsampled.
- Keep license/source details synchronized with `docs/ASSET_LIBRARY.md`.
